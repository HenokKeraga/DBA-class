1) create a table called family with the following columns and data types:

familyid int
first_name varchar(50)
last_name varchar(50)
gender_name char(1)
birtday datetime
email varchar(100)

2) After you create the table, insert 5 rows into your table using the insert into command.

3) After you insert data into your table, make various updates of your columns using the update your table name.

4) Write a select statement to view data from the family table you have created using the 

Select your columns
from your table
where filter criteria.

solution
---------